{
  "name": "openclaw-at0mic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -i src/input.css -o css/tailwind.css --minify",
    "build": "npm run build:css && mkdir -p dist && cp index.html CNAME dist/ && cp -r css js img dist/",
    "dev": "npx tailwindcss -i src/input.css -o css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4",
    "autoprefixer": "^10.4",
    "postcss": "^8.4",
    "@tailwindcss/forms": "^0.5",
    "@tailwindcss/container-queries": "^0.1"
  }
}
